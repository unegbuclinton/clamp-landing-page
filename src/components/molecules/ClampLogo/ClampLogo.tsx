import Logo from '@/assets/svgs/website-logo.svg'
import Clamp from '@/assets/svgs/Clamp.svg'

const ClampLogo = () => {
  return (
    <header className=' flex gap-[13px] items-center'>
      <Logo />
      <Clamp />
    </header>
  )
}

export default ClampLogo
